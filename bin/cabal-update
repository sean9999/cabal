#!/usr/bin/env bash

CABAL_ROOT=$HOME/.config/serf
source $CABAL_ROOT/bin/vars

cd $CABAL_ROOT
git add --all . \
	&& commit -m "wip" \
	&& git pull \
	&& git push \
	&& cabal-binify \
	&& cabal-reload
